# ListenBar

ListenBar 展示本机监听端口及其所属进程，并支持检查、忽略和管理这些进程。

## Language

**监听端点（PortEntry）**：进程在本机使用的协议、地址与端口组合。一个监听进程可以拥有多个监听端点。

**监听进程组（PortProcessGroup）**：菜单中归在一起的监听端点；属于同一 App 的辅助进程可以归入同一个组，命令行进程按 PID 区分。

**扫描快照（PortScanSnapshot）**：一次成功扫描得到的监听端点、进程信息和监听进程组。它包含被忽略的进程；扫描失败不代表监听端点为空。

**忽略项（IgnoredProcessItem）**：按 App 身份或绝对可执行文件路径隐藏监听进程组的规则，不依赖某次运行的 PID。恢复表示移除忽略规则，不表示重新启动进程。

**监听可见性（PortVisibility）**：扫描快照在当前已应用的忽略规则下呈现的监听端点、进程信息及进程组。菜单打开期间变更忽略规则，在菜单关闭后才改变这份呈现。

**进程详情（PortProcessDetails）**：供查看、复制和定位使用的进程路径、启动命令、来源及内存信息。启动命令的摘要用于展示，完整文本用于复制；原始命令与脱敏命令保持区分。

**App 路径（applicationPath）**：监听进程组所属的外层 App 位置；存在相互冲突的位置时，不选择其中一个。它不同于组内辅助进程的可执行文件路径。

**根菜单（Root menu）**：不隶属于其他菜单、且不是应用主菜单的菜单。

**菜单跟踪期（Menu tracking）**：用户正在操作根菜单的期间，监听列表和更新标题各自保留稳定的呈现。子菜单的跟踪结束不代表根菜单的跟踪结束。
